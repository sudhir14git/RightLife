<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/songImage"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp"
        android:background="@drawable/logo_rightlife"/>

    <LinearLayout
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/songTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lorem Ipsum"
            android:fontFamily="@font/dmsans_bold"
            android:maxLength="20"
            android:ellipsize="end"
            android:textColor="@color/txt_color_journal_date"
            android:textSize="16sp"/>

        <TextView
            android:id="@+id/songSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lorem Ipsum"
            android:fontFamily="@font/dmsans_regular"
            android:textSize="12sp"
            android:textColor="@color/txt_color_journal_date"/>
    </LinearLayout>

    <ImageView
        android:id="@+id/playingIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_pause_player_new"
        android:visibility="visible"/>
    <ImageView
        android:id="@+id/deleteIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/breathing_green_tick"
        android:visibility="visible"/>
</LinearLayout>
