<!-- res/drawable/progress_layer.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background layer (unfilled part) -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <corners android:radius="1dp"/> <!-- Rounded corners for the background -->
            <solid android:color="#A2A2A2"/>
        </shape>
    </item>

    <!-- Progress layer (filled part) -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="1dp"/> <!-- Rounded corners for the progress -->
                <solid android:color="@color/white"/>
            </shape>
        </clip>
    </item>

</layer-list>
